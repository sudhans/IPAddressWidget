<?xml version="1.0" encoding="utf-8"?>
<resources>


    <style name="Theme.IpAppWidget.AppWidgetContainerParent" parent="@android:style/Theme.DeviceDefault">
        <!-- Radius of the outer bound of widgets to make the rounded corners -->
        <item name="appWidgetRadius">16dp</item>
        <!--
        Radius of the inner view's bound of widgets to make the rounded corners.
        It needs to be 8dp or less than the value of appWidgetRadius
        -->
        <item name="appWidgetInnerRadius">8dp</item>
    </style>

    <style name="Theme.IpAppWidget.AppWidgetContainer" parent="Theme.IpAppWidget.AppWidgetContainerParent">
        <!-- Apply padding to avoid the content of the widget colliding with the rounded corners -->
        <item name="appWidgetPadding">8dp</item>
    </style>

    <style name="Theme.DeviceDefault.DayNight" parent="android:Theme.Material.Light.NoActionBar" />
</resources>